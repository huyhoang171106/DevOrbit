spring:
  application:
    name: devorbit-api
  datasource:
    url: jdbc:postgresql://localhost:5432/devorbit_db
    username: postgres
    password: huyhoang
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080

