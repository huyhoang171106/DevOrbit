spring:
  application:
    name: devorbit-api
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: devorbit.project@gmail.com 
    password:  sycfmtwhcyqbfzcw 
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  sql:
    init:
      mode: always
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/devorbit_db}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:huyhoang}
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: ${JPA_FORMAT_SQL:true}

app:
  github:
    token: ${GITHUB_TOKEN:}
  jwt:
    secret: ${JWT_SECRET:default-jwt-secret-key-long-enough-256bits}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:120}

server:
  port: ${SERVER_PORT:8080}