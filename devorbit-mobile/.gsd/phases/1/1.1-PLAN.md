---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Database & Network Foundation

## Objective
Establish the core data infrastructure for the Academic OS, including the Room database for offline caching and the Retrofit networking layer for backend synchronization.

## Context
- .gsd/SPEC.md
- .gsd/phases/1/RESEARCH.md
- app/src/main/java/vn/edu/uit/devorbit/mobile/network/ApiService.kt

## Tasks

<task type="auto">
  <name>Setup Room Database & Entities</name>
  <files>
    - app/src/main/java/vn/edu/uit/devorbit/mobile/data/local/DevOrbitDatabase.kt
    - app/src/main/java/vn/edu/uit/devorbit/mobile/data/local/entity/CourseEntity.kt
    - app/src/main/java/vn/edu/uit/devorbit/mobile/data/local/entity/RepoEntity.kt
    - app/src/main/java/vn/edu/uit/devorbit/mobile/data/local/dao/CourseDao.kt
    - app/src/main/java/vn/edu/uit/devorbit/mobile/data/local/dao/RepoDao.kt
  </files>
  <action>
    - Create the Room database with entities for Courses and Repositories as defined in RESEARCH.md.
    - Implement DAOs with basic CRUD and 'upsert' functionality for sync.
    - Ensure foreign key constraints between Repos and Courses.
  </action>
  <verify>.\gradlew :app:assembleDebug (Confirming build passes with new entities)</verify>
  <done>Room database is initialized and DAOs are functional.</done>
</task>

<task type="auto">
  <name>Implement Sync Repository</name>
  <files>
    - app/src/main/java/vn/edu/uit/devorbit/mobile/data/repository/AcademicRepository.kt
    - app/src/main/java/vn/edu/uit/devorbit/mobile/di/RepositoryModule.kt
  </files>
  <action>
    - Create a Repository that orchestrates data flow between ApiService and Room.
    - Implement a 'refreshCourses()' and 'refreshRepos(courseId)' function using the stale-while-revalidate pattern.
    - Register the repository in Hilt modules.
  </action>
  <verify>Check Hilt dependency injection for AcademicRepository in a test or logs.</verify>
  <done>Repository layer successfully mediates between network and local storage.</done>
</task>

## Success Criteria
- [x] Room database can store and retrieve course/repo data.
- [x] API responses are successfully cached into Room.
- [x] App builds and DI completes without errors.

## Execution Log

| Task | Status | Date | Notes |
|------|--------|------|-------|
| Setup Room Database & Entities | ✅ Done | 2026-04-30 | Created `DevOrbitDatabase.kt` with 4 entities: CourseEntity, CourseRelationshipEntity, RepoEntity, TaskEntity. DAOs: CourseDao, RepoDao, RelationshipDao, TaskDao with upsert/CRUD operations. Foreign key constraints between Repos and Courses. |
| Implement Sync Repository | ✅ Done | 2026-04-30 | Created `AcademicRepository.kt` orchestrating ApiService and Room flows. Implemented `refreshCourses()`, `refreshRepos()`, `getCourseGraph()` with stale-while-revalidate pattern. Registered in Hilt (RepositoryModule.kt + DataModule.kt). Additional repositories created: AuthRepositoryImpl, DiscoveryRepositoryImpl, ResourceRepositoryImpl, BookmarkRepositoryImpl, AiRepositoryImpl. |

## Verification
- Room database initialized at `devorbit.db` with version tracking
- DI completes via Hilt (DataModule provides Retrofit, Room, OkHttp; RepositoryModule provides repositories)
- Build verified: `./gradlew :app:assembleDebug` passes
- 9 domain engines also built on this foundation (KnowledgeGraphEngine, BurnoutEngine, GpaEngine, RiskEngine, SimulationEngine, RecommendationEngine, StudyPlannerEngine, TaskBreakdownEngine, WorkloadEngine)
