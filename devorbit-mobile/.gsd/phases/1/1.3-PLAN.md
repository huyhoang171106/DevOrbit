---
phase: 1
plan: 3
wave: 2
---

# Plan 1.3: High-Density Course Hub

## Objective
Rebuild the Course and Repository discovery experience focusing on productivity and search-first UX.

## Context
- .gsd/SPEC.md
- .gsd/DECISIONS.md
- app/src/main/java/vn/edu/uit/devorbit/mobile/data/repository/AcademicRepository.kt

## Tasks

<task type="auto">
  <name>Implement High-Density Course List</name>
  <files>
    - app/src/main/java/vn/edu/uit/devorbit/mobile/ui/screen/courses/CourseListScreen.kt
    - app/src/main/java/vn/edu/uit/devorbit/mobile/ui/components/CourseListItem.kt
  </files>
  <action>
    - Build a high-density List View for UIT courses using Room data.
    - Implement a persistent search bar at the top (Search-first UX).
    - Add filtering by Semester and Department if available in data.
  </action>
  <verify>Search for a specific course code (e.g., 'IT001') and verify it appears instantly from cache.</verify>
  <done>Course Hub allows fast, searchable access to all UIT courses.</done>
</task>

<task type="auto">
  <name>Integrate Galaxy Explorer Toggle</name>
  <files>
    - app/src/main/java/vn/edu/uit/devorbit/mobile/ui/screen/courses/CourseHubScreen.kt
  </files>
  <action>
    - Implement a Floating Action Button or Toggle to switch between List and Galaxy views.
    - Integrate the existing GalaxyGraphCanvas.kt as the "Explorer" mode.
    - Ensure theme consistency between the list and graph views.
  </action>
  <verify>Switch between List and Galaxy modes; verify Galaxy Graph renders without crashing.</verify>
  <done>Galaxy Graph is successfully integrated as a secondary discovery feature.</done>
</task>

## Success Criteria
- [x] Users can find courses via high-speed local search.
- [x] Data is populated from Room (API → Room → UI flow).
- [x] List View and Galaxy View are swappable.

## Execution Log

| Task | Status | Date | Notes |
|------|--------|------|-------|
| Implement High-Density Course List | ✅ Done | 2026-04-30 | Created `CourseListScreen.kt` with search bar, semester filter chips, high-density list items. Uses Room data via `CourseDao`. CourseHubNavigationState.kt created for detail navigation state. CourseDetailScreen.kt shows per-course details. RepoListSection.kt renders repo list per course. |
| Integrate Galaxy Explorer Toggle | ✅ Done | 2026-04-30 | Implemented in `CourseHubScreen.kt` with toggle between List View and Galaxy Graph view. GalaxyGraphCanvas.kt integrated as the Explorer mode with semester-based grid layout (column=semester, row=index), HK headers, course code labels, completion rings, and simulation-fail indicators. |

## Verification
- CourseHubScreen renders with semester statistics header, filter chips, and list/graph toggle
- Search filters courses by code/name instantly from Room cache
- Data flows: API -> Room -> UI (stale-while-revalidate via AcademicRepository)
- Galaxy Graph Canvas shows semester-based grid with interactive nodes
- CourseDetailScreen shows course info, prerequisites tab, related courses
