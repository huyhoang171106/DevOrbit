export interface DevOrbitFeature {
  title: string;
  subtitle: string;
  description: string;
  icon: string; // emoji
  color: string; // accent color hex
  gradient: string; // gradient pair for card
}
