# Auto-review script config
# Copy thành .env và điền giá trị

# Bắt buộc
LLM_API_KEY=your-key-here

# Optional — tăng GitHub rate limit (5000 req/h thay vì 60)
GITHUB_TOKEN=

# Mặc định localhost:8080, đổi nếu server ở host khác
API_BASE=http://localhost:8080
